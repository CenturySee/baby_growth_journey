services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: baby_growth_journey
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - baby_data:/app/data

volumes:
  baby_data:
